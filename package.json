{
  "name": "shopdeck-lms",
  "version": "1.0.0",
  "scripts": {
    "build": "cd client && npm install && npm run build && cd ..",
    "start": "node server/server.js"
  },
  "engines": {
    "node": "18.x"
  }
}
